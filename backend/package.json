{
  "name": "socket-image-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Express + Socket.IO server that accepts image payloads and logs them.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.1"
  }
}
